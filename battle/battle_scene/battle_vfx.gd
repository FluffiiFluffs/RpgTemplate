class_name BattleVFX
extends Node2D
##battle_vfx.gd
##Keeps a list of constants (packedscenes) which are instantiated onto an Actor within the scene. PackedScenes are simply effects animations that automatically play when instantiated and then queue_free


func play_vfx(target, effect)->void:
	pass
